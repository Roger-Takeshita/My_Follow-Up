import React from 'react';
import FormApplication from '../../components/FormApplication/FormApplication';

function NewApplicationPage({ history }) {
    return <FormApplication history={history} />;
}

export default NewApplicationPage;
